# Dependencies
node_modules/

# Machine-generated lockfiles
package-lock.json

# Recorded API responses — byte-exact fixtures, never reformat
tests/fixtures/

# Field notes: local, git-ignored, one long line per event by design.
# The file, not the directory — anything else left in .revloop/ is still checked.
.revloop/field-notes.md
