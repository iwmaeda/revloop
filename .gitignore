node_modules/
.DS_Store

# Per-developer agent state
.claude/settings.local.json
.claude/*.local.json

# Per-developer toolchain overrides (see mise.toml)
mise.local.toml

# Files the loop writes at runtime: field notes, grading input, the worktree
# ledger (see docs/design-notes.md)
.revloop/
